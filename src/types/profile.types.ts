export interface IProfile {
  id: string;
  name: string;
  email: string;
  // image?: string;
  // bio?: string;
  // createdAt: string;
  // updatedAt: string;
  // isActive?: boolean;
  // isAdmin?: boolean;
  // isEmailVerified?: boolean;
}