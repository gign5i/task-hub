import type {Config} from "tailwindcss";

const config: Config = {
  theme: {
    extend: {
      color: {
        // gradient_color_1: "#f3eeff",
        gradient_color_1: "#444ddd",
        // gradient_color_2: "#f6f5ff",
        gradient_color_2: "#f63ddd",

      }
    }
  }
}

export default config